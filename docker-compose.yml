@@ -1 +1,54 @@services: backend: build: ./backend image: sentinel-iomt-backend:latest restart: unless-stopped ports: - "4000:4000" environment: NODE_ENV: production PORT: 4000 JWT_SECRET: ${JWT_SECRET:?Set JWT_SECRET in .env before starting} ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:5173,http://localhost:8080,https://iomt-ddos-system.onrender.com} DB_PATH: /app/db_data/sentinel.db volumes: - db_data:/app/db_data - backend_logs:/app/logs healthcheck: test: ["CMD", "node", "-e", "fetch('http://localhost:4000/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"] interval: 30s timeout: 5s retries: 3 start_period: 10s frontend: build: context: ./frontend args: VITE_API_BASE: ${PUBLIC_API_BASE:-http://localhost:4000, https://iomt-ddos-system.onrender.com} image: sentinel-iomt-frontend:latest restart: unless-stopped ports: - "8080:80" depends_on: backend: condition: service_healthy 
#One-off batch job — does not run by default. 
Invoke explicitly with: docker compose --profile pipeline run --rm ml-pipeline ml-pipeline: build: ./ml-pipeline image: sentinel-iomt-ml-pipeline:latest profiles: ["pipeline"] environment: PIPELINE_DATA_DIR: /app/data PIPELINE_RESULTS_DIR: /app/results PIPELINE_BACKEND_DATA_DIR: /app/backend_data volumes: - ./ml-pipeline/data:/app/data - ./ml-pipeline/results:/app/results - ./backend/data:/app/backend_datavolumes: db_data: backend_logs:
